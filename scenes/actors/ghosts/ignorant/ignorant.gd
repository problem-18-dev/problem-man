extends Ghost


#func _update_pathing() -> void:
	#super()
	#
	#if move_points.size() > 8:
		#return
	#
	#var corner_cell := corners[ScatterCorner.BOTTOM_LEFT]
	#move_points = maze.get_move_points(current_cell, corner_cell)
	#nav_preview_line.points = move_points
